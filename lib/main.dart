import 'package:flutter/material.dart';

import 'app.dart';

var routes = <String, WidgetBuilder>{
  // "/home": (BuildContext context) => HomeScreen(),
  // "/intro": (BuildContext context) => IntroScreen(),
};

void main() => runApp(App4CarApp());
